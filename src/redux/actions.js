// actions.js
// Action creator for incrementing the counter
export const increment = () => ({
  type: 'INCREMENT', // Action type indicating an increment operation
});

// Action creator for decrementing the counter
export const decrement = () => ({
  type: 'DECREMENT', // Action type indicating a decrement operation
});
